import requests


def get_weather(city):
    res = requests.get(f"https://wttr.in/{city}?format=3", timeout=5)
    if res.status_code != 200:
        return "Weather API is acting up again."
    return res.text
